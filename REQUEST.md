PlaNU 프론트엔드의 현재 구현 상태를 분석하고, 화면 간 연결과 실제 동작을 완성해 주세요.

## 작업 목표

현재 프론트엔드는 개별 화면 UI는 일부 구현되어 있지만, 각 화면이 실제 사용자 흐름에 따라 연결되지 않았거나 mock 데이터로 동작하는 부분이 있습니다.

이번 작업의 목표는 다음과 같습니다.

1. GitHub의 프론트 관련 브랜치와 현재 `main` 브랜치를 모두 조사
2. 이미 구현된 화면과 파일의 역할 파악
3. 필요한 프론트 작업만 안전하게 통합
4. 각 화면의 역할과 화면 간 이동 흐름을 문서로 작성
5. 단순 UI가 아니라 실제 네비게이션, 상태 전달, API 호출이 동작하도록 구현

---

# 1. 브랜치 조사 및 통합 원칙

GitHub 원격 브랜치 중 이름이 `backend`로 시작하지 않는 브랜치를 모두 확인해 주세요.

단, 확인 없이 모든 브랜치를 바로 병합하지 마세요.

각 브랜치에 대해 다음을 먼저 조사해 주세요.

* 브랜치 이름
* `main`에 이미 병합되었는지
* 변경된 파일
* 추가하거나 수정한 화면
* 현재 코드와 충돌하는지
* mock 또는 미완성 코드가 포함되어 있는지
* 현재 사용자 흐름에 필요한 작업인지

현재 확인된 프론트 관련 PR에는 다음 작업이 있습니다.

* PR #13: `GuideScreen`
* PR #14: `Department selection screen`

이미 `main`에 반영된 브랜치는 다시 병합하지 마세요.

아직 반영되지 않은 비백엔드 브랜치는 변경 내용을 검토한 뒤, 현재 PlaNU 화면 흐름에 필요한 코드만 새 작업 브랜치에 통합해 주세요.

오래된 코드, 중복 코드, 실험용 코드, 현재 설계와 맞지 않는 코드는 무조건 병합하지 말고 제외 이유를 보고해 주세요.

## 권장 작업 브랜치

```text
frontend/connect-screen-flow
```

현재 `main`에서 새 브랜치를 만들고 필요한 변경만 통합해 주세요.

---

# 2. 현재 확인된 프론트 파일

현재 `main`에서 확인되는 파일은 다음과 같습니다.

## `frontend/lib/main.dart`

현재 Flutter 앱의 진입점입니다.

현재 확인된 동작:

* `MaterialApp` 생성
* PlaNU 테마 적용
* 시작 화면으로 `GuideScreen` 사용

현재 구조를 유지하되, 전체 화면 상태나 라우팅 구조가 필요하다면 적절한 방식으로 리팩터링해 주세요.

## `frontend/lib/screens/guide_screen.dart`

PlaNU 시작 전 수강편람 준비와 서비스 이용 전 주의사항을 안내하는 화면입니다.

현재 확인된 동작:

* 부산대학교 학생지원시스템 외부 링크 열기
* 수강편람 다운로드 방법 화면으로 이동
* 전공 수강편람 및 교양선택 파일 안내
* 전공 과목 사전 준비 안내
* 30분 세션 안내
* 개인정보 및 파일 삭제 안내

현재 문제:

* `다음` 버튼의 기본 동작이 비어 있음
* `onNext`가 전달되지 않으면 아무 동작도 하지 않음
* 학과 입력 화면과의 실제 연결을 확인하고 구현해야 함

## `frontend/lib/screens/catalog_download_guide_screen.dart`

부산대학교 학생지원시스템에서 수강편람을 내려받는 방법을 안내하는 화면입니다.

현재 `guide_screen.dart`에서 `Navigator.push()`로 연결되어 있습니다.

이 화면의 뒤로 가기 동작과 안내 화면 복귀가 정상인지 확인해 주세요.

## 학과 선택 화면 파일

PR #14와 현재 브랜치들을 조사하여 학과 선택 화면의 정확한 파일명을 확인해 주세요.

추측해서 새 파일을 만들지 말고, 이미 구현된 파일이 있다면 해당 파일을 사용하세요.

확인 후 문서에 다음 형식으로 실제 파일명을 기록해 주세요.

```text
frontend/lib/screens/<실제 학과 화면 파일명>.dart
```

현재 학과 선택은 mock 데이터로 동작한다고 알려져 있습니다.

mock 데이터가 어느 파일에 선언되어 있는지 찾아서 문서에 기록하고, 실제 데이터 연결 대상으로 교체해 주세요.

---

# 3. 먼저 작성할 화면 흐름 문서

다음 파일을 새로 만들어 주세요.

```text
docs/frontend_screen_flow.md
```

이 문서는 일반적인 기획 문서가 아니라, 현재 저장소의 실제 파일을 기준으로 작성해야 합니다.

각 화면마다 다음 내용을 포함해 주세요.

```text
화면 이름
실제 구현 파일
현재 구현 여부
현재 연결된 이전 화면
현재 연결된 다음 화면
화면의 역할
입력받는 데이터
다음 화면에 전달하는 데이터
호출하는 API
성공 시 동작
실패 시 동작
현재 mock 또는 미완성 부분
구현 완료 조건
```

이미 구현된 화면은 반드시 실제 파일명으로 작성하세요.

아직 존재하지 않는 화면은 다음처럼 표시하세요.

```text
현재 파일 없음
권장 파일명: frontend/lib/screens/major_input_screen.dart
```

기존 파일이 있는데 비슷한 새 파일을 중복 생성하지 마세요.

---

# 4. PlaNU의 최종 화면 흐름

프론트 전체 사용자 흐름은 다음과 같아야 합니다.

```text
PlaNU 안내 화면
→ 수강편람 다운로드 상세 안내
→ 학과 입력 화면
→ 전공 수강편람 및 전공 프롬프트 입력 화면
→ 전공 시간표 검증 화면
→ 필요 시 전공 과목 직접 선택 화면
→ 교양 프롬프트 및 추천 템플릿 선택 화면
→ 시간표 생성 중 화면
→ 추천 시간표 후보 화면
```

수강편람 다운로드 상세 안내 화면은 필수 직선 흐름이 아니라 안내 화면에서 선택적으로 진입했다가 돌아오는 보조 화면입니다.

실제 핵심 흐름은 다음과 같습니다.

```text
GuideScreen
→ 학과 입력
→ 전공 정보 입력
→ 전공 검증
→ 교양 조건 입력
→ 후보 생성
→ 결과
```

---

# 5. 화면별 역할과 연결 구현

## 5.1 안내 화면

기존 파일:

```text
frontend/lib/screens/guide_screen.dart
```

역할:

* PlaNU 이용 전 준비사항 안내
* 부산대 학생지원시스템 연결
* 수강편람 다운로드 방법 안내
* 전공 시간표를 미리 준비해야 한다는 안내
* 세션 만료 및 개인정보 안내

필요한 수정:

* `다음` 버튼을 실제 학과 입력 화면과 연결
* `onNext ?? () {}`처럼 아무 동작도 하지 않는 기본 구현 제거
* 버튼을 누르면 실제 화면 이동이 발생해야 함
* 필요하다면 `Navigator`, named route, `GoRouter` 중 현재 프로젝트 구조에 가장 적절한 한 가지 방식을 선택
* 라우팅 방식을 혼합하지 말 것

## 5.2 수강편람 다운로드 방법 화면

기존 파일:

```text
frontend/lib/screens/catalog_download_guide_screen.dart
```

역할:

* 부산대학교 학생지원시스템에서 전공 수강편람을 내려받는 방법 설명

필요한 확인:

* `guide_screen.dart`에서 정상 진입 가능한지
* 뒤로 가기를 통해 안내 화면으로 정상 복귀하는지
* 외부 링크 실행 실패 시 사용자 안내가 있는지
* 화면이 전체 핵심 흐름의 다음 단계로 잘못 사용되고 있지는 않은지

## 5.3 학과 입력 화면

기존 파일은 PR #14와 원격 브랜치를 조사해 실제 파일명을 확인하세요.

역할:

* 사용자의 학과명을 문자열로 입력받음
* 교양필수 분반 제한 대상 학과는 자동 완성 제공
* 자동 완성에 없는 학과도 직접 입력 가능

중요한 동작:

* 자동 완성 데이터는 모든 부산대 학과 목록이 아님
* 분반 제한 데이터에서 추출된 학과를 자동 완성에 사용함
* 자동 완성 결과가 없다고 입력값을 거부하면 안 됨
* 빈 문자열만 막고 사용자가 직접 입력한 학과명은 허용
* 선택한 학과를 다음 화면에 전달

현재 mock 처리:

* mock 학과 데이터가 선언된 정확한 파일과 변수명을 찾기
* 실제 `departments.json`이 존재한다면 해당 파일로 교체
* 실제 JSON 위치도 문서에 기록
* 파일이 없다면 임의의 학과 목록을 새로 작성하지 말고 필요한 데이터 파일이 없다고 보고

다음 연결:

```text
학과 입력 완료
→ 전공 수강편람 및 전공 프롬프트 입력 화면
```

## 5.4 전공 정보 입력 화면

기존 파일이 있는지 모든 비백엔드 브랜치와 `main`에서 확인하세요.

없다면 다음 이름을 우선 검토하세요.

```text
frontend/lib/screens/major_input_screen.dart
```

역할:

* 선택한 학과 표시
* 전공 수강편람 `.xlsx` 업로드
* 수강하려는 전공 과목 및 분반 프롬프트 입력

API 흐름:

```text
POST /catalog/major
→ session_id 저장
→ POST /major/preview
→ 전공 시간표 검증 화면
```

필요한 상태:

* department
* majorCatalogFile
* majorPrompt
* sessionId
* majorPreview

입력값 검증:

* 파일 미선택
* 프롬프트 공백
* 잘못된 확장자
* 파일 용량 초과
* 손상된 파일
* 네트워크 오류

API 성공 전에 다음 화면으로 이동하지 마세요.

## 5.5 전공 시간표 검증 화면

기존 파일이 있는지 조사하세요.

없다면 권장 파일명:

```text
frontend/lib/screens/major_preview_screen.dart
```

역할:

* LLM과 수강편람을 통해 매칭된 전공 과목을 사용자가 검증
* 자동 결과를 사용자 확인 없이 확정하지 않음

표시할 데이터:

* matched_courses
* ambiguous_courses
* unmatched_courses
* has_time_conflict
* can_confirm
* 과목명
* 분반
* 교수
* 강의 시간
* 강의실
* 총 학점

버튼:

```text
이대로 확정
직접 선택하기
다시 입력하기
```

`이대로 확정` 동작:

```text
POST /major/confirm
→ 교양 조건 입력 화면
```

`can_confirm == false`이면 확정 버튼을 비활성화하세요.

## 5.6 전공 과목 직접 선택 화면

기존 파일이 있는지 조사하세요.

없다면 권장 파일명:

```text
frontend/lib/screens/major_course_selection_screen.dart
```

역할:

* 자동 매칭 결과가 부정확할 때 파싱된 전공 과목과 분반을 사용자가 직접 선택

중요:

현재 백엔드가 직접 선택한 과목을 확정하는 API를 제공하는지 먼저 확인하세요.

지원 API가 없다면 프론트에서 mock으로 완료 처리하지 마세요.

대신 다음 내용을 보고하세요.

* 필요한 API 요청 구조
* 프론트에서 전달해야 하는 과목 식별자
* 현재 `/major/confirm`으로 재사용 가능한지
* 백엔드 변경이 필요한지

백엔드 계약이 불명확하면 임의 구현하지 마세요.

## 5.7 교양 조건 및 템플릿 화면

기존 파일이 있는지 조사하세요.

없다면 권장 파일명:

```text
frontend/lib/screens/general_preference_screen.dart
```

역할:

* 교양 조건 프롬프트 입력
* 추천 템플릿 선택
* 교양 영역 선택
* 교양선택 수강편람 선택 업로드
* 목표 총 학점 입력
* 추가 교양 과목 수 입력

템플릿 매핑:

```text
균형형 → balanced
공강 우선 → free_day_priority
오전 수업 최소화 → no_morning_priority
수업 시간 압축 → compact_schedule
```

API 흐름:

```text
POST /general/prepare
→ POST /recommend/generate
→ POST /recommend/rank
```

## 5.8 시간표 생성 중 화면

기존 파일이 있는지 조사하세요.

없다면 권장 파일명:

```text
frontend/lib/screens/timetable_loading_screen.dart
```

역할:

* 연속 API 호출 진행 상태 표시
* 중복 버튼 요청 방지
* 오류 발생 시 입력값을 유지한 채 교양 조건 화면으로 복귀

진행 단계:

```text
교양 과목 준비 중
→ 시간표 후보 생성 중
→ 추천 순위 계산 중
```

## 5.9 추천 시간표 결과 화면

기존 파일이 있는지 조사하세요.

없다면 권장 파일명:

```text
frontend/lib/screens/timetable_result_screen.dart
```

역할:

* 상위 시간표 후보 표시
* 후보별 시간표와 세부 정보 비교
* 템플릿 변경
* 교양 조건 수정
* 처음부터 다시 시작

템플릿만 변경할 때:

```text
POST /recommend/rank
```

다시 호출하면 안 되는 API:

```text
POST /general/prepare
POST /recommend/generate
```

교양 프롬프트, 목표 학점 또는 교양 개수가 바뀌면 후보를 다시 생성해야 합니다.

---

# 6. 상태 관리

화면마다 별도의 mock 값을 만들어 넘기지 마세요.

하나의 사용자 흐름 상태를 정의하고 화면 간에 공유해 주세요.

필요한 상태 예시:

```text
department
sessionId
majorCatalogFile
majorPrompt
majorPreview
confirmedMajorCourses
electiveCatalogFile
electiveArea
preferencePrompt
selectedTemplate
targetTotalCredits
additionalElectiveCount
generatedCandidates
rankedCandidates
```

현재 프로젝트 규모에 맞는 단순한 상태 관리 방식을 사용하세요.

새로운 대형 상태 관리 라이브러리는 꼭 필요한 경우가 아니라면 추가하지 마세요.

`Navigator` 생성자 전달, 상위 StatefulWidget, ChangeNotifier 등 현재 구조에 가장 단순하고 일관된 방식을 선택하세요.

---

# 7. API 연결

백엔드의 실제 라우트와 schema를 확인한 뒤 프론트 모델을 작성하세요.

사용할 API:

```text
POST /catalog/major
POST /major/preview
POST /major/confirm
POST /general/prepare
POST /recommend/generate
POST /recommend/rank
```

다음 항목을 구현하거나 기존 파일을 확인하세요.

* API base URL 설정
* multipart 파일 업로드
* JSON 요청
* JSON 응답 모델
* 공통 오류 응답 모델
* 로딩 상태
* 세션 만료 처리
* 네트워크 오류 처리

API 서비스 파일이 이미 존재하면 새로 만들지 말고 기존 파일을 사용하세요.

없다면 현재 프로젝트 구조를 분석하여 적절한 위치에 생성하고 문서에 기록하세요.

---

# 8. 공통 오류 처리

백엔드에서 다음 오류가 반환되면 사용자 흐름에 맞게 처리하세요.

## 세션 만료

```text
SESSION_NOT_FOUND
```

표시 문구:

```text
세션이 만료되었습니다.
수강편람 파일을 다시 업로드해 주세요.
```

동작:

```text
프론트 흐름 상태 초기화
→ GuideScreen 이동
```

## 파일 오류

* MAJOR_CATALOG_REQUIRED
* INVALID_FILE_EXTENSION
* INVALID_EXCEL_FILE
* FILE_TOO_LARGE

현재 입력 화면에 오류를 표시하고 입력값을 유지하세요.

## 단계 오류

```text
INVALID_SESSION_STAGE
```

사용자가 잘못된 화면 순서로 접근했는지 확인하고, 정상적인 이전 단계로 이동할 수 있도록 처리하세요.

---

# 9. 구현 원칙

* 화면 UI만 만들고 연결을 미루지 마세요.
* 새 화면을 만들면 이전 화면과 다음 화면의 연결을 같은 작업에서 구현하세요.
* 버튼은 실제 네비게이션, API 호출 또는 상태 변경 중 하나를 수행해야 합니다.
* `onPressed: () {}`와 같은 빈 동작을 남기지 마세요.
* mock 데이터는 실제 데이터로 교체하거나 명확한 TODO와 사유를 남기세요.
* API 성공 전에 다음 화면으로 이동하지 마세요.
* API 실패 시 입력값이 사라지지 않게 하세요.
* 이전 화면으로 이동했을 때 기존 입력을 유지하세요.
* 기존 디자인을 불필요하게 변경하지 마세요.
* 이미 구현된 컴포넌트와 화면을 최대한 재사용하세요.
* 비슷한 역할의 화면이나 파일을 중복 생성하지 마세요.

---

# 10. 테스트 및 검증

최소한 다음 사용자 흐름을 실행 가능하게 만들어 주세요.

```text
GuideScreen
→ 학과 입력
→ 전공 파일과 프롬프트 입력
→ 전공 미리보기
→ 전공 확정
→ 교양 조건과 템플릿 선택
→ 시간표 생성
→ 결과 확인
```

다음을 추가로 확인하세요.

* 안내 화면의 `다음` 버튼이 동작함
* 다운로드 안내 화면에서 정상 복귀함
* 학과 자동 완성이 실제 JSON을 사용함
* 자동 완성에 없는 학과도 입력 가능함
* 화면을 뒤로 이동해도 입력값이 유지됨
* API 호출 중 중복 요청이 발생하지 않음
* 오류 시 현재 입력이 유지됨
* 세션 만료 시 처음 화면으로 복귀함
* 템플릿 변경 시 후보를 다시 생성하지 않고 재랭킹만 함

Flutter 정적 분석과 테스트를 실행하세요.

```bash
cd frontend
flutter analyze
flutter test
```

가능하다면 전체 흐름에 대한 widget test도 작성하세요.

---

# 11. 작업 완료 후 보고

작업이 끝나면 다음 내용을 정리해 주세요.

1. 조사한 모든 비백엔드 브랜치
2. 각 브랜치의 병합 여부와 역할
3. 실제로 통합한 브랜치
4. 통합하지 않은 브랜치와 제외 이유
5. 현재 존재하는 화면 파일 목록
6. 새로 만든 화면 파일 목록
7. 각 화면의 이전·다음 연결
8. 제거한 mock 데이터
9. 아직 백엔드 계약이 필요한 기능
10. 실행한 테스트와 결과
11. `docs/frontend_screen_flow.md`에 기록한 전체 흐름

기존 파일을 조사하지 않고 추측으로 새 화면을 대량 생성하지 마세요.

먼저 저장소의 실제 파일과 브랜치를 조사하고 문서를 작성한 뒤, 그 문서를 기준으로 화면 연결과 실제 동작을 구현해 주세요.

